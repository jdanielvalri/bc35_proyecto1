spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: clientebanco

  application:
    name: microcliente
  cloud:
    config:
      uri: http://localhost:8084
      username: root
      password: s3cr3t